## Packages
date-fns | Formatting timestamps (e.g., "5 mins ago", "MMM dd, yyyy")

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
Dashboard should poll /api/stats and /api/logs every 5000ms.
